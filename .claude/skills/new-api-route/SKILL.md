---
name: new-api-route
description: Add a default-deny TSOA route with @Security and an auth-posture allow-list entry.
---

# new-api-route

Add a default-deny TSOA route with @Security and an auth-posture allow-list entry.

> Sanitized stub. In the private source repo this skill carries the full operational
> playbook (commands, gates, evidence format). The fleet's routing map references it by name;
> `.claude/scripts/validate-agent-fleet.mjs` asserts it resolves.
